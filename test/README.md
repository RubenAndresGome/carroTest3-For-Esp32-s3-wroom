# Pruebas de firmware

Directorio reservado para pruebas automatizadas de PlatformIO. Las pruebas
manuales que reemplazan temporalmente el firmware viven en `src/` y deben tener
un nombre `test_<función>.cpp`; solamente una puede permanecer activa.
