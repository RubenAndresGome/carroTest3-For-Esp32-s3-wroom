# Interfaces del firmware

Cabeceras compartidas por los módulos de `src/`. Mantener aquí estructuras,
contratos y constantes; las implementaciones pertenecen en `src/`.

`Secrets.example.h` es la única plantilla de credenciales versionable.
`Secrets.h` debe existir solo localmente y está excluido por `.gitignore`.
