#pragma once

// Copia este archivo como Secrets.h y reemplaza ambos valores localmente.
// Estos placeholders vacios son intencionalmente invalidos: el firmware debe
// fallar al compilar si se intenta usar esta plantilla sin configurarla.
// Secrets.h esta excluido por .gitignore y nunca debe confirmarse en Git.
constexpr char WIFI_AP_SSID[] = "";
constexpr char WIFI_AP_PASSWORD[] = "";
